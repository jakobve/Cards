<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".ui.fragment.CardsApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cards"
        tools:targetApi="31">
        <activity
            android:name=".ui.activity.SettingsActivity"
            android:exported="false"
            android:label="@string/title_activity_settings" />
        <activity
            android:name=".ui.activity.TutorialActivity"
            android:exported="false"
            android:label="@string/tutorial_nav_menu_title" />
        <activity
            android:name=".ui.activity.StatisticsActivity"
            android:exported="false"
            android:label="@string/statistics_nav_menu_title" />
        <activity
            android:name=".ui.activity.TitleActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>